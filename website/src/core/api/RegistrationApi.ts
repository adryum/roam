export class RegistrationApi {
    async logIn(email: string, password: string) {
        try {
            // await rquests to server
            
            return "all ok"
        } catch (error) {
            
        }
        
    }
}